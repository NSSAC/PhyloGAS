inputs:
  - name: va_delta_ref_data
    metadata: data/va_delta_ref.metadata.csv
    aligned: data/va_delta_ref.sequences.fasta

builds:
  va-delta-ref-build:
    subsampling_scheme: all

filter:
  skip_diagnostics: true
  min_length: 10
