inputs:
  - name: syn_gen_proportional
    metadata: data/run_03_with_delta2.metadata.tsv.xz
    aligned: data/run_03_with_delta2.sequences.fasta.xz

builds:
  mandy-model-delta10:
    subsampling_scheme: my_custom_scheme
    auspice_config: config/auspice_augmented.json

filter:
  skip_diagnostics: true # Some quality control still takes place
  min_length: 10
  group-by: region country division county year month sex latitude longitude age_group smh_race

# TreeTime settings
refine:
  root: "Wuhan-Hu-1/2019"
  clock_rate: 0.0008
  clock_std_dev: 0.0004
  coalescent: "opt"
  date_inference: "marginal"
  divergence_unit: "mutations"
  clock_filter_iqd: 8

traits:
  mandy-model-delta10: ### build name
    sampling_bias_correction: 2.5
    columns: ["region", "country", "division", "county"]

subsampling:
  my_custom_scheme:
    county:
      # other subsampling parameters...
      max_sequences: 1000
      include: data/reference_id.fasta

files:
  # auspice_config: config/auspice_augmented.json
  lat_longs: config/va_lat_longs.tsv  
  # Not used when "aligned" is used for the sequences
  # alignment_reference: data/va_delta_ref.sequences.fasta

# Haven't tried with this yet
#nextclade_dataset: data/va_delta_ref_dataset.zip
